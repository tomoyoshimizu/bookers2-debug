$(".comment-index").html("<%= escape_javascript(render partial: "comments/show", collection: @book.comments, as: "comment") %>");
$(".comment-counter").html("<%= escape_javascript(render "comments/counter", comment: @book.comments) %>");
$(".comment-form").val("");