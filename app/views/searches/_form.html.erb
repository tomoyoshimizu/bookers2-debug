<%= form_with url: search_path, method: :get, local: true do |f| %>
  <%= f.text_field :search_word %>
  <%= f.select :search_model, ["User", "Book"] %>
  <%= f.select :search_type, ["部分一致", "完全一致", "前方一致", "後方一致"] %>
  <%= f.submit "検索" %>
<% end %>